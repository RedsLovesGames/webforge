export function sitemapUrls(xml){return [...String(xml||'').matchAll(/<loc>\s*([^<]+?)\s*<\/loc>/gi)].map(m=>m[1].replace(/&amp;/g,'&'));}
export function slugFromUrl(url){const u=new URL(url);return u.pathname.split('/').filter(Boolean).at(-1)||u.hostname.replace(/^www\./,'');}
export function titleFromSlug(slug){return String(slug).split(/[-_]+/).filter(Boolean).map(x=>x[0]?.toUpperCase()+x.slice(1)).join(' ');}
export function discoverFromSitemap(xml,source,{includePaths=[]}={}){const urls=sitemapUrls(xml).filter(url=>!includePaths.length||includePaths.some(p=>new URL(url).pathname.includes(p)));const seen=new Set(),items=[];for(const url of urls){const name=slugFromUrl(url);if(!name||seen.has(name))continue;seen.add(name);let type='component';const pathname=new URL(url).pathname;if(pathname.includes('/block/'))type='block';else if(pathname.includes('/template'))type='template';else if(pathname.includes('/dashboard'))type='dashboard';const address=source.registryTemplate?source.registryTemplate.replace('{name}',name):(source.installPrefix?`${source.installPrefix}${name}`:name);items.push({name,title:titleFromSlug(name),type:`registry:${type}`,description:'',url,installAddress:address});}return items;}
